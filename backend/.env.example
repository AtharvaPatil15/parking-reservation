# Backend environment (P4-01). Copy to .env and fill in. Timings/weights live in the DB (D8), not here.
NODE_ENV=development
PORT=4000

# PostgreSQL (points at the local-postgres container)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/parking_poc?schema=public

# Redis (used by BullMQ jobs — later phase)
# REDIS_URL=redis://localhost:6379

# Auth (JWT). Set strong secrets in real environments.
JWT_ACCESS_SECRET=dev-access-secret-change-me
JWT_REFRESH_SECRET=dev-refresh-secret-change-me
ACCESS_TOKEN_TTL=900
REFRESH_TOKEN_TTL=604800

# CORS origin of the frontend
CORS_ORIGIN=http://localhost:5173

LOG_LEVEL=info
