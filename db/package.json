{
  "name": "parking-poc-db",
  "version": "1.0.0",
  "private": true,
  "description": "Bootstrap: create the Parking POC database + schema in the Postgres container",
  "scripts": {
    "create-schema": "node create-schema.js"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
